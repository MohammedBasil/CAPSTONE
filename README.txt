The sample files requested to try the program are contained in the "SavedFiles" folder.
In order to use them please remember to put all the .bin files in the compilation folder.
For simplicity of use, all passwords have been set to "Password".

If there are no files when the program is started a default administrator will be used. 
In order to remove all data simply erase .bin files from the compilation folder.